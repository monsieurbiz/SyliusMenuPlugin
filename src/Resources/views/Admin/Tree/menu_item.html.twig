{% set tree = hookable_metadata.context.tree %}
<div>
    <a class="link-offset-2 link-offset-3-hover link-underline link-underline-opacity-0 link-underline-opacity-75-hover" href="{{ path('monsieurbiz_menu_admin_menu_item_update', {'id': tree.id, 'menuId': tree.menu_id }) }}">
        {{ tree.name }}
    </a>
</div>
